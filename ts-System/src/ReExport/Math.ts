const {
    random,
    sign, abs,
    min, max,
    pow, sqrt,
    floor, ceil, trunc, round,
    sin, cos, tan, atan2,
    fround, imul,
    log: ln, log10, log2,
    PI: pi,
} = Math;

export const tau = 2 * pi;
export {
    random,
    sign, abs,
    min, max,
    pow, sqrt,
    floor, ceil, trunc, round,
    sin, cos, tan, atan2,
    fround, imul,
    ln, log10, log2,
    pi,
};
