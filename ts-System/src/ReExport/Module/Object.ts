const {
    create, 
    defineProperty, defineProperties, hasOwn, 
    freeze, 
    assign, entries,
    getPrototypeOf, setPrototypeOf,
    getOwnPropertyDescriptor,
} = Object;

const {
    ownKeys,
} = Reflect;

export {
    create, 
    defineProperty, defineProperties, hasOwn, 
    freeze, 
    assign, entries,
    getPrototypeOf, setPrototypeOf,
    getOwnPropertyDescriptor,
    
    ownKeys,
}
